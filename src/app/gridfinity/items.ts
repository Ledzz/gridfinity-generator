import { baseplate } from "../../manifold/baseplate/baseplate.ts";
import { box } from "../../manifold/box/box.ts";
import { baseplateExtend } from "../../manifold/baseplate-extend/baseplateExtend.ts";

export const RENDER = {
  baseplate,
  baseplateExtend,
  box,
};
